<%
    let value = artifact.summary;
    if(!value || value.length < 1) {
        if (lang.id === 'en') {
            value = artifact.summary || episode.summary
        } else {
            value = await askAI(`Translate into ${lang.name}: ${episode.summary}`);
        }
    }
%>
<%- value %>
